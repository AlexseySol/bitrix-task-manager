{
  "name": "bitrix-task-manager",
  "version": "0.1.0",
  "description": "Universal MCP server for Bitrix24 task management scoped to the webhook owner.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node ./dist/server.js",
    "configure": "node ./scripts/configure.mjs",
    "verify": "node ./scripts/verify-webhook.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.18"
  },
  "license": "MIT"
}
